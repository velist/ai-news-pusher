# AI新闻翻译系统依赖
# 主要使用Python标准库，少量外部依赖用于增强功能

# 核心依赖 - 用于HTTP请求和JSON处理
requests>=2.25.0

# 环境变量管理
python-dotenv>=0.19.0

# 可选依赖 - 用于高级功能
# beautifulsoup4>=4.9.0  # 网页解析（如需要）
# lxml>=4.6.0           # XML解析（如需要）